# Configuration file for run-structured.py
# Lines starting with # are comments and will be ignored
# Format: key=value (no spaces around the = sign)
# Values can be quoted with single or double quotes if they contain spaces

# CSV file containing model names
models_csv=/data/nkhajehn/watcher-mcp-server/llm_pipeline/services/models_combined_with_num_predict.csv

# Directory containing Jinja templates
templates_dir=/data/nkhajehn/watcher-mcp-server/llm_pipeline/templates/ftp_prompts

# Specific templates to use (comma-separated, or leave empty/use "all" for all templates)
# Examples:
# template=security1.jinja,security-incremental-1.jinja
# template=all
# template=
template=prompt6.jinja,prompt4.jinja,prompt7.jinja


# Starting iteration number
start_iteration=1

# Number of iterations to run
num_iterations=1

# Number of trials per model
trials_per_model=10

# Target device name
target=File Transfer Protocol

# Interface type
interface=serial AT-command interface

timeout=600

model_row=0
